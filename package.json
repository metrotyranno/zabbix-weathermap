{
  "name": "zabbix-weathermap",
  "version": "1.0.0",
  "description": "A program that creates a weathermap to be used as a background in zabbix maps.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brandon van der Loo",
  "license": "CC-BY-NC-SA-4.0",
  "type": "module",
  "dependencies": {
    "axios": "^0.26.1",
    "canvas": "^2.9.1",
    "node-cron": "^3.0.0"
  }
}
